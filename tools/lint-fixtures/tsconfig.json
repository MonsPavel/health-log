{
  "$comment": "TASK-003 §19: конфиг фикстур линта. Фикстуры вне основного графа tsc -b; отдельный tsconfig нужен, чтобы typed-linting (projectService) ассоциировал их с проектом. Прецедент — tools/tsconfig-fixture из TASK-002. types не ограничен: @types/node из корня резолвит node:* в фикстурах (иначе no-unsafe-* губят «чистые» фикстуры).",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "allowImportingTsExtensions": true,
    "noEmit": true
  },
  "include": ["**/*.ts"]
}
