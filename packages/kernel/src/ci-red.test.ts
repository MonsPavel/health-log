// TASK-014 §20.2: контрольный падающий тест для красного PR — удаляется после приёмки.
import { describe, expect, it } from 'vitest';

describe('TASK-014 §20.2: контрольный красный тест', () => {
  it('падает намеренно', () => {
    expect(1).toBe(2);
  });
});
