/**
 * TASK-006 §5: порог «мало данных» — честного отказа ИИ от обобщений (FR-5.4).
 * Живёт в kernel, чтобы правило существовало в одном месте: общий порог Analytics
 * и AI Insight (арх. 02 §3.2).
 */

/** Минимум измерений за период, ниже которого ИИ отказывается от обобщений (FR-5.4). */
export const AI_MIN_MEASUREMENTS = 7;

/** Минимум дней, охваченных измерениями, ниже которого ИИ отказывается (FR-5.4). */
export const AI_MIN_DAYS = 3;
