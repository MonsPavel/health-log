/**
 * TASK-016 §7: рука измерения. Строковый union — представление невалидной руки
 * типом исключено; отдельная фабрика не нужна (нет доменного кода ошибки — §5).
 * ARM_VALUES — источник значений для CHECK в DDL (TASK-025) и zod-схемы (TASK-028)
 * с комментарием-синхронизацией сюда.
 */
export type Arm = 'left' | 'right';

/** Все допустимые значения Arm в каноническом порядке. */
export const ARM_VALUES: readonly Arm[] = ['left', 'right'];
