/**
 * Экран-заглушка каркаса (TASK-007 §10).
 *
 * «Health Log» — имя продукта, не переводимый контент (§17); i18n-ключ появится
 * с TASK-013. Ссылка ниже — заглушка для ручной проверки блокировки внешней
 * навигации (§20, шаг 5).
 */
export function App(): JSX.Element {
  return (
    <main>
      <h1>Health Log</h1>
      <p>
        <a href="https://example.org/">Внешняя ссылка — навигация должна блокироваться</a>
      </p>
    </main>
  );
}
